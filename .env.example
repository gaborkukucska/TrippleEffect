# START OF FILE .env.example

# --- Core Provider API Keys & URLs ---
# Fill in keys ONLY for the providers you intend to use.
# TrippleEffect will check these at startup based on agent configurations.

# OpenAI API Key (Required if using any OpenAI models)
OPENAI_API_KEY=your_openai_api_key_here
# Optional: Specify OpenAI base URL if using a proxy or alternative endpoint
# OPENAI_BASE_URL=

# OpenRouter API Key (Required if using any OpenRouter models)
OPENROUTER_API_KEY=your_openrouter_api_key_here
# Optional: Override OpenRouter base URL
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# Optional: Set your custom OpenRouter Referer (otherwise defaults are used)
# OPENROUTER_REFERER=http://your-app-url-or-name

# Ollama Base URL (Required if using any Ollama models)
# Usually http://localhost:11434 or http://<ollama_server_ip>:11434
OLLAMA_BASE_URL=http://localhost:11434

# --- *** NEW: LiteLLM Configuration *** ---
# LiteLLM Base URL (Required if using LiteLLM provider - not implemented yet, but preparing)
# Usually http://localhost:8000 or http://<litellm_server_ip>:<port>
LITELLM_BASE_URL=http://localhost:4000
# Optional: LiteLLM API Key if your LiteLLM instance requires one
# LITELLM_API_KEY=your_litellm_api_key

# --- *** NEW: Model Tier Selection *** ---
# Selects which models to consider based on cost tier (mainly for OpenRouter).
# Valid values: "FREE", "ALL" (default)
# - FREE: Only uses models identified as free (e.g., matching ':free' suffix on OpenRouter, all local models).
# - ALL: Considers all discovered models from configured providers.
MODEL_TIER=ALL

# --- Default Agent Configuration ---
# These are used if an agent's config in config.yaml omits specific values.
DEFAULT_AGENT_PROVIDER=openrouter
DEFAULT_AGENT_MODEL="google/gemini-flash-1.5:free"
DEFAULT_SYSTEM_PROMPT="You are a helpful assistant."
DEFAULT_TEMPERATURE=0.7
DEFAULT_PERSONA="Assistant Agent"

# --- Project/Session Storage ---
# Optional: Specify a different base directory for project/session data.
# Defaults to 'projects' directory within the TrippleEffect root.
# PROJECTS_BASE_DIR=

# --- Tool Configuration ---
# Required for GitHub Tool functionality
GITHUB_ACCESS_TOKEN=your_github_personal_access_token_here

# --- Logging Configuration (Optional) ---
# LOG_LEVEL=DEBUG # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
